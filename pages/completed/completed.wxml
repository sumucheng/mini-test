<!--pages/completed/completed.wxml-->
<view wx:for="{{list}}" wx:for-item="item" wx:key="id" class="item">
  <view class="title">
    <text>{{item.completedTime}}</text>
    <view bindtap="deleteTodoItem" id="{{item.tag}}">×</view>
  </view>
  <view class="text">{{item.value}}</view>
</view>